<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="mrgenco.leanmeetingapp.ModeratorActivity">

    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/nameEditText"
        android:hint="Name"
        android:layout_alignParentTop="true"
        android:layout_marginRight="31dp"
        android:layout_marginEnd="31dp" />

    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/emailEditText"
        android:layout_below="@+id/nameEditText"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:hint="Email"
        android:layout_marginTop="5dp"
        android:inputType="textEmailAddress"/>

    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/ticketIDEditText"
        android:layout_below="@+id/emailEditText"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:hint="Ticket ID"
        android:layout_marginTop="5dp"
        android:inputType="number"/>

    <Button
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="INVITE"
        android:id="@+id/inviteAttendantsButton"
        android:onClick="inviteAttendant"
        android:textColor="#38b44c"
        android:layout_below="@+id/ticketIDEditText"
        android:layout_alignRight="@+id/ticketIDEditText"
        android:layout_alignEnd="@+id/ticketIDEditText" />
    <Button
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="SHOW ATTENDANTS"
        android:id="@+id/showAttendantsButton"
        android:onClick="showAttendants"
        android:layout_above="@+id/addMeetingNotesButton"
        android:layout_centerHorizontal="true" />

    <Button
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="ADD MEETING NOTES"
        android:id="@+id/addMeetingNotesButton"
        android:onClick="addMeetingNotes"
        android:textColor="#F93C2B"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true" />

</RelativeLayout>
